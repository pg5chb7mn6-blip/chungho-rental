# 청호렌탈 사이트 SEO 개편 — 배포 방법

이 폴더 전체가 그대로 GitHub 저장소 `pg5chb7mn6-blip/chungho-rental` 의 **최상단(root)** 에 올라가야 합니다.

---

## 0. 올리기 전에 반드시 채워야 할 값 (중요)

아래 표시가 남아 있으면 검색엔진과 이용자 모두에게 신뢰도가 떨어지고, 전자상거래법·개인정보보호법상 표시 의무를 채우지 못합니다.
`index.html`, `company/index.html`, `privacy/index.html`, 그리고 모든 하위 페이지의 푸터에 들어 있습니다.

| 플레이스홀더 | 채울 내용 | 왜 필요한가 |
|---|---|---|
| `{{대표자명}}` | 대표자 성명 | 전자상거래법 제13조 사업자 정보 표시 |
| `{{사업자등록번호}}` | 000-00-00000 | 동일 |
| `{{통신판매업신고번호}}` | 제0000-지역-0000호 | 신고 대상인 경우. 미신고 상태면 이 줄 자체를 삭제하세요 (허위 표시 금지) |
| `{{사업장 주소}}` | 소비자 불만 처리가 가능한 실제 주소 | 동일 |
| `{{대표전화}}` | 상담 전화번호 | 동일 |
| `{{사내 이메일}}` | 상담·개인정보 문의 수신 이메일 | 개인정보 보호책임자 연락처 |
| `{{네이버_서치어드바이저_인증코드}}` | 아래 2번에서 발급 | 네이버 사이트 소유확인 |
| `{{구글_서치콘솔_인증코드}}` | 아래 3번에서 발급 | 구글 사이트 소유확인 |

**한 번에 바꾸는 방법 (권장):** 이 폴더에서 아래 명령을 실행하세요.

```bash
# macOS
grep -rl '{{대표자명}}' . | xargs sed -i '' 's/{{대표자명}}/홍길동/g'
# Linux / Git Bash
grep -rl '{{대표자명}}' . | xargs sed -i 's/{{대표자명}}/홍길동/g'
```
나머지 항목도 같은 방식으로 치환하면 됩니다. 값에 `/`가 들어가면 `s|...|...|g` 형태로 구분자를 바꾸세요.

`index.html` 안에는 전화번호를 넣는 자리가 하나 더 있습니다. `const PHONE = "";` 를 찾아 `const PHONE = "010-0000-0000";` 처럼 채우면, 폼 전송이 실패했을 때 전화 상담 링크가 자동으로 뜹니다.

---

## 1. GitHub 저장소에 올리기

### 방법 A — 웹에서 (터미널 없이)
1. https://github.com/pg5chb7mn6-blip/chungho-rental 접속
2. **Add file → Upload files**
3. 이 폴더의 **모든 파일과 폴더**를 통째로 끌어다 놓기
   - 기존 `index.html`은 덮어쓰기됩니다 (새 버전이 SEO 패치본입니다)
   - `data.js`, `images.js`, `admin.html`은 기존과 동일하니 그대로 두어도 됩니다
   - 폴더(`jeongsugi-rental/` 등)는 브라우저에서 폴더째 드래그하면 구조가 유지됩니다
4. Commit 메시지 입력 후 **Commit changes**

### 방법 B — 터미널
```bash
git clone https://github.com/pg5chb7mn6-blip/chungho-rental.git
cd chungho-rental
cp -R /이/폴더/경로/* .
git add -A
git commit -m "SEO 개편: 정적 랜딩 12종, sitemap/robots/rss, 구조화 데이터, 사업자 정보 및 개인정보처리방침 추가"
git push
```

배포에는 보통 1~3분 걸립니다. 아래 주소가 모두 열리는지 확인하세요.

- https://pg5chb7mn6-blip.github.io/chungho-rental/
- https://pg5chb7mn6-blip.github.io/chungho-rental/jeongsugi-rental/
- https://pg5chb7mn6-blip.github.io/chungho-rental/sitemap.xml
- https://pg5chb7mn6-blip.github.io/chungho-rental/robots.txt

---

## 2. 네이버 서치어드바이저 등록 (네이버 노출의 출발점)

1. https://searchadvisor.naver.com 접속 → 네이버 로그인
2. **웹마스터도구 → 사이트 관리 → 사이트 등록**
   `https://pg5chb7mn6-blip.github.io/chungho-rental/` 입력
3. **소유확인** — "HTML 태그" 방식을 선택하면 `<meta name="naver-site-verification" content="...">` 코드가 나옵니다.
   그 content 값을 복사해 이 폴더 전체의 `{{네이버_서치어드바이저_인증코드}}` 자리에 치환하고 다시 커밋 → 서치어드바이저에서 "소유확인" 클릭
4. **요청 → 사이트맵 제출**: `sitemap.xml` 입력
5. **요청 → RSS 제출**: `rss.xml` 입력
6. **요청 → 웹페이지 수집**: 주요 페이지 URL을 하나씩 직접 넣어 수집 요청 (하루 할당량이 있으니 중요 페이지부터)
7. **검증 → 웹사이트 최적화 도구** 실행 후 경고가 뜨면 그 항목만 손보면 됩니다

> 네이버는 등록 후 실제 검색 반영까지 대략 2주 안팎이 걸립니다. 안 뜬다고 중간에 설정을 계속 바꾸지 말고 기다리세요.

### 왜 이 사이트에 정적 페이지 12개를 새로 만들었나
네이버 검색로봇(Yeti)은 **자바스크립트를 실행하지 않습니다.** 기존 사이트는 제품·요금이 전부 `data.js`에서 자바스크립트로 그려지기 때문에, 네이버 입장에서는 사실상 **빈 페이지**로 보였습니다. 그래서 자바스크립트 없이도 읽히는 정적 HTML 안내 페이지를 12개 만들고, 메인 페이지에도 정적 텍스트 섹션(`#lineup`)을 추가했습니다. 견적 계산기는 그대로 두고, 검색엔진에게 보여줄 몸통을 따로 붙인 구조입니다.

---

## 3. 구글 서치콘솔 등록

1. https://search.google.com/search-console 접속
2. **속성 추가 → URL 접두어** → `https://pg5chb7mn6-blip.github.io/chungho-rental/`
3. 소유확인은 **HTML 태그** 방식 선택 → content 값을 `{{구글_서치콘솔_인증코드}}` 자리에 치환 후 커밋 → 확인
4. **Sitemaps** 메뉴에서 `sitemap.xml` 제출
5. **URL 검사**에서 주요 페이지를 하나씩 넣고 "색인 생성 요청"

Bing 웹마스터도구(https://www.bing.com/webmasters)도 같은 방식으로 등록해두면 좋습니다. 서치콘솔에서 데이터를 그대로 가져올 수 있어 5분이면 끝납니다.

---

## 4. 커스텀 도메인 (되도록 권장)

`pg5chb7mn6-blip.github.io` 는 무작위 문자열이 섞인 공유 도메인이라, 검색엔진과 이용자 모두에게 "개인 테스트 페이지"로 보이기 쉽습니다. 상담·계약을 유도하는 상업 사이트라면 자체 도메인이 신뢰도 면에서 유리합니다.

1. 도메인 구입 (가비아·후이즈 등, `.co.kr` 기준 연 2만원 안팎)
   - **주의:** `청호나이스`만 단독으로 쓴 도메인(예: `chunghonais.co.kr`)은 본사 공식몰로 오인시킬 소지가 있어 상표·부정경쟁방지 측면에서 위험합니다. `niceeng-rental.co.kr` 처럼 **본인 상호를 포함한 도메인**을 권합니다.
2. DNS에 CNAME 또는 A 레코드 설정 → GitHub 저장소 Settings → Pages → Custom domain 입력 → Enforce HTTPS 체크
3. 도메인을 붙였다면 `build.py`의 `PREFIX = "/chungho-rental"` 을 `PREFIX = ""`, `ORIGIN`을 새 도메인으로 바꿔 다시 생성하거나, 파일 전체에서 기존 주소를 새 주소로 일괄 치환하세요.
4. 도메인 변경 후에는 서치어드바이저·서치콘솔에 **새 주소로 다시 등록**해야 합니다.

---

## 5. 이번에 추가·변경된 파일

| 경로 | 내용 |
|---|---|
| `index.html` | 견적 흐름은 그대로. 메타태그·구조화 데이터 보강, 히어로에 **빠른 견적 칩 6종** 추가, **URL 딥링크 프리셋** 지원, 하단에 상세 안내 링크 섹션 `#lineup` 추가(견적 흐름 뒤로 배치), 개인정보 동의 문구 법정 요건에 맞게 재작성, 광고 수신 동의 분리, 사업자 정보 푸터 |
| `jeongsugi-rental/` 외 11개 폴더 | 정적 랜딩·가이드 페이지 (자바스크립트 없이 읽힘) |
| `company/` | 사업자 정보 및 이용 안내 |
| `privacy/` | 개인정보처리방침 (11개 항목, 국외이전 고지 포함) |
| `robots.txt` | 크롤러 허용 + Yeti·Daum 명시, admin.html 색인 제외, 사이트맵 위치 |
| `sitemap.xml` | 15개 URL |
| `rss.xml` | 네이버 서치어드바이저 RSS 제출용 |
| `404.html` | 없는 주소로 들어와도 안내 페이지로 유도 |
| `og.png` | 카카오톡·페이스북 공유 시 뜨는 미리보기 이미지 |

`data.js`, `images.js`, `admin.html`은 손대지 않았습니다.

---

## 5-1. 고객이 바로 견적 낼 수 있게 만든 장치

**① 히어로 빠른 견적 칩** — 첫 화면 카드 아래에 `얼음정수기 / 냉온정수기 / 공기청정기 / 비데 / 연수기 / 제습기` 버튼을 넣었습니다. 하나만 누르면 **가정용 · 신규 · 방문관리 · 60개월** 조건이 자동으로 채워지고 제품 목록과 월 렌탈료로 바로 스크롤됩니다. 5번 고르던 것이 1번이 됩니다. 조건을 바꾸고 싶으면 기존처럼 고객 유형부터 다시 고르면 됩니다.

**② URL 딥링크** — 견적 계산기가 주소창 파라미터를 읽어 단계를 미리 채웁니다.

```
/?c=개인&s=S&m=방문관리&o=60&g=얼음정수기#quote
```

| 파라미터 | 값 |
|---|---|
| `c` | 개인 · 기업 · 교육기관 |
| `s` | S(신규) · J(재렌탈) · P(패키지) · H(행사) |
| `m` | 방문관리 · 자가관리 |
| `o` | 36 · 48 · 60 · 72 · 전체 |
| `g` | 얼음정수기 · 냉온정수기 · 냉정수기 · 일반정수기 · 공기청정기 · 비데군 · 연수기군 · 제습기 · 휘카페 · 전체 |

값이 잘못되었거나 해당 조건에 제품이 없으면 그 단계 앞에서 자동으로 멈추므로 빈 화면이 뜨지 않습니다.

**블로그 글에 이 주소를 그대로 붙이세요.** 예를 들어 비데 B360 글이라면 `...?c=개인&s=S&m=방문관리&o=60&g=비데군#quote` 를 링크하면, 글을 읽고 넘어온 사람이 클릭 한 번에 비데 렌탈료를 봅니다. 상세 안내 페이지 12종에는 이미 각 제품에 맞는 딥링크가 **1분 견적 시작** 버튼으로 들어가 있습니다.

---

## 5-2. 고객에게 보이는 이름을 "청호나이스"로 통일했습니다

고객이 "나이스엔지니어링"을 계열사로 오해할 여지를 없앴습니다.

- 로고·상단·페이지 제목·공유 미리보기: **청호나이스 렌탈**
- 본문 표현: "청호나이스 공식 판매점"
- 상호 `나이스엔지니어링`은 **푸터 사업자 정보 한 줄과 저작권 표기에만** 남겼습니다

상호를 완전히 지우지 않은 이유가 있습니다. 전자상거래법 제13조는 상호·대표자·사업자등록번호 표시를 의무로 정하고 있어 뺄 수 없습니다. 또 브랜드명만 내세우고 운영 주체를 감추면 본사 공식몰로 오인시킨 것이 되어 부정경쟁방지법상 영업주체 혼동에 걸릴 수 있습니다. 지금 구조 — **눈에 보이는 브랜드는 청호나이스, 법정 표시는 푸터에** — 가 혼란을 줄이면서 법적 리스크도 피하는 지점입니다.

---

## 6. 자주 생기는 문제

**Q. 폴더 주소 끝에 `/`를 빼면 404가 뜹니다.**
GitHub Pages는 `/jeongsugi-rental` 도 `/jeongsugi-rental/` 로 자동 리디렉션합니다. 정상입니다.

**Q. 수정했는데 반영이 안 됩니다.**
GitHub Pages는 빌드에 1~3분 걸립니다. 브라우저 강력 새로고침(Ctrl+Shift+R)도 해보세요.

**Q. 상담 신청이 안 들어옵니다.**
`index.html` 의 `FORM_ENDPOINT`(구글 앱스스크립트 주소)가 살아 있는지 확인하세요. 앱스스크립트를 다시 배포하면 주소가 바뀝니다. 전송이 실패하면 견적 내용이 자동으로 복사되고 카카오톡으로 보내라는 안내가 뜨도록 되어 있습니다.

**Q. 개인정보를 구글 서버에 저장해도 되나요?**
개인정보보호법 제28조의8상 국외 이전에 해당할 수 있어, 이번 개편에서 동의 문구와 개인정보처리방침에 국외 이전 사실을 명시해 두었습니다. 다만 근본적으로는 국내 서버 기반 폼으로 옮기는 편이 안전합니다. 문의가 늘어나면 검토해 보세요.
